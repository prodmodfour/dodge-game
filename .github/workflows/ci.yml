name: ci

on:
  push:
  pull_request:

jobs:
  validate-autobuild:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run shell/security quality gate
        env:
          UNITY_QUALITY_MODE: skip
        run: bash scripts/quality-gate.sh
